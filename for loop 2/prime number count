#include <stdio.h>
int main()
{
int n, decimal = 0, base = 1, digit;
scanf("%d", &n);
for(; n > 0; n = n / 10){
        digit = n % 10;
        decimal = decimal + digit * base;
        base = base * 2;
    }
    printf("%d", decimal);
    return 0;
}
