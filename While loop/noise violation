#include<stdio.h>
int main()
{
    char ch[50];
    int n;
    scanf("%d",&n);
    for(int i=0;i<=n;i++){
    scanf("%c",&ch[i]);
    }
    while(n!=0){
        if(n < 70)
        {
            printf("noise violation : %d\n",n);
        }
        else if(n >= 70){
            printf("longest violation streak : %d\n",n);
        }
        else{
            printf("noise violation : %d\n",n);
            printf("longest violation streak : %d\n",n);
        }
    }
    return 0;
}
